import { memo } from "react";
import { Route, Routes } from "react-router-dom";


const AppRouter = () => {
  return (
    <Routes>
      <Route />
    </Routes>
  );
};

export default memo(AppRouter);